{{- if .Values.configMap.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "test-app.fullname" . }}
  labels:
    randomKey: "{{ .Values.randomKey }}"
    {{- include "test-app.labels" . | nindent 4 }}
data:
  {{- toYaml .Values.configMap.data | nindent 2 }}
{{- end }}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "example-patch.fullname" . }}
  labels:
    {{- include "example-patch.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "example-patch.selectorLabels" . | nindent 4 }}
